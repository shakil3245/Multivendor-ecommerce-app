import 'package:flutter/cupertino.dart';

class AttributeScreen extends StatelessWidget {
  const AttributeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Attribute"));
  }
}
